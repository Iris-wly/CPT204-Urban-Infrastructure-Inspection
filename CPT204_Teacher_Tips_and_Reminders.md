# CPT204 Group Project — 老师补充说明与重要提醒

> 来源：Session Slides（课堂补充讲解）

---

## 1. 让数据具有可解释性（Task A 计时表格）

报告中呈现的计时数据必须说明**获取方式**，建议做到以下三点：

- **重复测试取平均值**：每个排序算法建议运行多次（参考示例：1000次），取平均运行时间
- **Warm-up 预热阶段**：正式计时前先额外执行若干次（参考示例：50次）预热，排除 JVM 初始化、JIT 编译、内存分配等带来的初始噪声，确保记录的是**纯粹的排序时间**
- **结果四舍五入**：运行时间以毫秒（ms）为单位记录，保留 2～3 位小数，提升可读性

---

## 2. 认真对待 Follow-up 问题

任务书中有大量追问型问题，**追问部分才是得分重点**，例如：

- 如果图是无权图，可以考虑哪些替代图算法？它们与你当前选择相比如何？
- 三种排序算法中，哪一个在三个数据集上表现最稳定？**为什么？**
- 如果只能选一种排序算法用于最终系统，你会选最快的、最稳定的，还是最简单的？**为什么？**

> ⚠️ 不要只给出结论，**"Why"才是主要得分点**。

---

## 3. 探索性问题需要自学

部分问题涉及假设条件的变化，例如：

- 如果同时考虑运行效率和内存占用，算法选择会有什么影响？（需了解**空间复杂度**）
- 如果图规模变得很大，或者节点有坐标信息，哪些图算法或路径规划方案更合适？有哪些优缺点？

> 这类问题需要适当**自主查阅资料**（如空间复杂度、A\* 算法等）。  
> **替代算法无需实现代码**，只需在报告中分析讨论即可。

---

## 4. Task C：截图与 UML 图很重要

在 Task C（程序设计说明）中，图示内容能显著帮助评分老师理解设计，建议包含：

- 各类（Class）的职责说明
- 类与类之间如何协作完成完整的检查流程
- OOP 原则的具体应用说明，例如：**如何通过公共接口/抽象实现多态？**

> 代码截图 + UML 类图能有效展示设计，强烈建议加入报告。

---

## 5. Task D：AI 管理工具需要截图 + 说明

使用 Jira / Trello 等 AI 辅助项目管理工具时，报告中需要：

1. **截图**：展示任务看板 / 计划页面（作为证明）
2. **文字说明**：解释为什么这样规划、如何分配任务、如何协作等

> 只有截图没有说明是不够的，两者缺一不可。

---

## 6. 可选加分项（Icing on the cake）

以下内容**不强制要求**，但额外努力往往带来更高分数：

- 用**统计图表**（柱状图等）可视化算法效率对比
- 用 **JavaFX / Swing / AWT** 可视化图的路径结果
- 实际实现额外的替代算法（如 A\*、Floyd-Warshall 等）

> 🕐 请合理分配时间，兼顾其他课程的作业。

---

## 7. 最终提交提醒（Reminders）

| 项目 | 要求 |
|---|---|
| 提交文件数 | 4个：代码ZIP、Word报告、MP4视频、PPT |
| 截止时间 | **5月24日 23:59**（迟交每工作日扣5%，最多5天） |
| 封面页 | 必须有 |
| 报告页数限制 | 最多 **20页**（仅限 Chapter 1-4），超出内容放 Appendix |
| Chapter 5 代码 | 必须**复制粘贴**代码原文，**不可截图** |
| 视频时长 | 最长 **8分钟**，最大 **250MB** |
| 视频要求 | 两人都需出镜，各讲约 **4分钟** |
| 贡献分配 | 尽量贡献均等；个人最高得分 = 提交作品应得分数 |
| 提交人 | 只需**一名组员**提交全部4个文件 |
| 复用代码 | 允许参考已提供的排序/图算法代码，但需**批判性思考** |
| 答疑 | 欢迎携带报告/代码到 Office Hour 当面交流，效率更高 |


**完整库使用指引**：

---

## CW 可用 / 不可用库总结

### ✅ 明确可以用

这些都在课件代码和教材中出现过：

```java
// 数据存储
java.util.ArrayList
java.util.List
java.util.HashMap
java.util.Map
java.util.Set
java.util.HashSet

// 图算法必用
java.util.PriorityQueue      // Dijkstra 优化版用到
java.util.Collections        // Lab 10 答案中用了 Collections.reverse()

// 文件读取
java.io.BufferedReader
java.io.FileReader
java.io.IOException

// 系统工具
System.arraycopy()           // Merge Sort 课件中用到
System.currentTimeMillis()   // 计时
System.nanoTime()            // 计时（更精确）
Double.POSITIVE_INFINITY     // Dijkstra/Prim 初始化 cost 用到
Math.random()                // 课件示例用到

// 泛型与比较
java.util.Comparator         // Lab 9 泛型排序用到
```

---

### ❌ 明确不能用

```java
// 任何内置排序 —— Task A 必须手动实现
Arrays.sort()
Collections.sort()

// 任何第三方库
// Apache Commons, Guava, JGraphT 等一概不可用
```

---

### ⚠️ 需要注意的边界情况

**`Collections.reverse()`** 在 Lab 10 答案里出现了，说明可以用于**路径重建**（path reconstruction），但**不能**用它替代排序算法。

**`java.util.LinkedList`** 在教材中作为队列用于 BFS，也是安全的。

---

### 一句话原则

> 只要是 CPT204 课上讲过、课件代码里出现过、或 Liang 教材里有的标准 Java 库，都可以用。**排序算法必须自己手写**，这是 Task A 的核心要求。